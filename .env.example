# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to Git (it's already in .gitignore)
# ============================================

# Supabase Configuration (Frontend)
# These variables are exposed to the browser (VITE_ prefix)
VITE_SUPABASE_URL=your-supabase-url-here
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase Configuration (Backend - for vercel dev)
# These variables are only accessible in serverless functions
SUPABASE_URL=your-supabase-url-here
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# OpenAI API Key (Backend only)
OPENAI_API_KEY=your-openai-api-key-here

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your actual values in .env.local
# 3. For local development with Vercel:
#    - Run: vercel dev
#    - Vercel will create/update .env.local automatically
# 4. For production (Vercel Dashboard):
#    - Set these variables in Vercel Dashboard → Settings → Environment Variables
# ============================================
